[project]
name = "specs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.22.0",
    "pytest>=8.0.0",
    "shared-schemas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["contracts"]
