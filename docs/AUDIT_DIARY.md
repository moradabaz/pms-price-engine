# Audit Diary

A chronological, phase-by-phase record of what has actually been done in this project, why, and what's left — written so that anyone picking this up (including a future version of whoever's reading this) can get oriented without reconstructing context from commit messages and conversation history that won't be around forever.

**How to use this document:**
- Read top to bottom for full history, or jump straight to [Where things stand right now](#where-things-stand-right-now) for immediate next steps.
- Each phase section links to its spec (`specs/phases/NN-name/spec.md`), relevant ADRs (`docs/adr/`), and incident write-ups (`error-handling/`) rather than duplicating their content — this diary is the map, not the territory.
- Update this file at the end of every phase (or sooner, if a session produces a non-obvious finding worth preserving) — it decays fast if it isn't kept current.

---

## Phase 0 — Repo setup

**Status:** Done. **Commit:** `848f1ad` ("chore: initial repository setup — streaming PoC structure"), refined by `42b8a36` (pandas/apache-flink dependency conflict fix).

Established the whole project skeleton before any single phase was implemented: the uv workspace (single lockfile across all Python services), the full `docker-compose.yml` (including services — Flink, LocalStack — that later phases haven't reached yet), the JSON Schema contracts under `specs/events/`, the ADR set (`docs/adr/ADR-0001`–`ADR-0004`), and CI (lint/typecheck/schema-contract validation on every push).

Key architectural decisions made here, still binding:
- **ADR-0001:** Kafka for `payment-events` (Debezium's native target), Kinesis for `market-price-events` (no CDC dependency) — deliberately mixed buses, not unified, both keyed by their respective business ID for per-entity ordering.
- **ADR-0002:** PyFlink over Java Flink — single-language (Python) stack.
- **ADR-0004:** pandas pinned to `2.2.x` workspace-wide to satisfy both `apache-flink` and `dashboard` under one lockfile.

---

## Phase 1 — Mock app & `payment_lines` DB

**Status:** Done, merged to `main` (PR #3, merge commit `60cfb16`). **Spec:** [`specs/phases/01-mock-app-db/spec.md`](../specs/phases/01-mock-app-db/spec.md).

**What was built:** `payment_lines.sql` (the authoritative source table + `dbz_publication`, wired into `docker-compose.yml` as a `docker-entrypoint-initdb.d` script) and `services/mock-pm-app` — a small generator that seeds ≥2 months of history across ≥10 apartments, then continuously inserts new rows (10–30s interval) and periodically flips `pending` rows to `paid`, simulating a live PM back-office.

**Key decisions:**
- **ADR-0003** was written *during* this phase, after discovering the originally-drafted `payment_line.v1` schema (nested `supplier`/`billing_period` objects, envelope metadata fields) couldn't actually be emitted by a flat-table CDC connector without extra reshaping infrastructure. Settled on: flat fields only, no CDC envelope metadata, `event_id` as a stable per-row UUID that consumers must upsert-by (not sum or append). This decision is load-bearing for every later phase that touches `payment_line.v1`.
- Phase 1's own §5 (Configuration) added concrete values for the mock app's env vars (`MOCK_APP_*`, `pydantic-settings`-based) and the Compose wiring — added in commit `ebb4604`, the same pattern this diary recommends for every future phase spec (decide configuration explicitly before implementation, don't leave it implicit).

**Non-obvious finding, fixed in-flight:** `pgoutput` produces *binary* logical-decoding output — the plain-text `pg_logical_slot_get_changes()` function errors against it (`ERROR: logical decoding output plugin "pgoutput" produces binary output`). Must use `pg_logical_slot_get_binary_changes()` or `pg_recvlogical` instead. Fixed in commit `7bf9bec`, discovered by actually running the AC-06 verification against the live stack rather than assuming the spec's originally-drafted command was correct.

**Residual limitation carried forward:** the manually-created replication slot used for Phase 1's own AC-06 must be dropped before Phase 2 registers its own (`debezium_payment_lines`) — an abandoned slot makes Postgres retain WAL indefinitely.

---

## Phase 2 — CDC pipeline (Postgres → Debezium → Kafka)

**Status:** Implemented — all seven acceptance criteria verified live against the running stack on 2026-07-16. **Spec:** [`specs/phases/02-cdc-pipeline/spec.md`](../specs/phases/02-cdc-pipeline/spec.md). Not yet merged to `main` (branch `phase-2-cdc-pipeline-config`).

**What was built:** `infra/debezium/postgres-connector.json` (the Debezium PostgreSQL source connector), the `payment-events.v1` Kafka topic (6 partitions, RF 1), and — discovered as a genuine requirement partway through, not planned upfront — `infra/debezium/custom-converters/`, a small Java project implementing Debezium's `CustomConverter` SPI to fix DATE column encoding (see below). Also added spec §6 (Configuration: topic partition count, creation command, connector-registration procedure) before implementation began, following Phase 1's precedent.

**This phase's core lesson, worth internalizing before working on any future CDC connector in this repo:** a Debezium connector reporting `"state": "RUNNING"` tells you almost nothing about whether it's actually delivering correct data. Every one of the four bugs below passed a naive health check. The only checks that actually caught them were: (1) counting messages on the *data plane* (`GetOffsetShell`, a live consumer), not the control plane (REST `/status`); (2) checking the Postgres replication slot's own lag (`pg_wal_lsn_diff`) directly; and (3) programmatically validating *every* live message against the real JSON Schema, not eyeballing one. Four full write-ups, each with a "situations you can hit this" section generalizing beyond our specific instance, live in [`error-handling/`](../error-handling/) — **read them before touching this connector's config again**:

1. **[`debezium-default-topic-naming-mismatch.md`](../error-handling/debezium-default-topic-naming-mismatch.md)** — Debezium's default topic name (`<prefix>.<schema>.<table>`) never matched the required `payment-events.v1`; nothing was flowing at all despite a healthy connector. Fixed with a `RegexRouter` transform.
2. **[`debezium-heartbeat-topic-stalls-entire-connector.md`](../error-handling/debezium-heartbeat-topic-stalls-entire-connector.md)** — `heartbeat.interval.ms` pointed at an auto-named topic that didn't exist; one unresolvable topic on a connector's *shared* producer stalls delivery to every topic it writes to, not just the affected one. Fixed by removing the setting (not needed here — the source table gets real writes constantly).
3. **[`debezium-date-decimal-wire-encoding-mismatch.md`](../error-handling/debezium-date-decimal-wire-encoding-mismatch.md)** — NUMERIC columns arrived as opaque base64 bytes, DATE columns as raw epoch-day integers, neither matching the JSON Schema contract. Fixed decimals with `decimal.handling.mode: double` (one config line); fixed dates by writing and compiling a real Debezium `CustomConverter` Java class (no bundled Kafka Connect SMT recognizes Debezium's own logical types — confirmed by testing, not assumption). Includes a postscript finding of its own: Kafka Connect persists offsets independent of the connector object's lifecycle, so fixing a connector's config does **not** retroactively fix already-published messages — a full offset reset (`stop` → `DELETE .../offsets` → topic recreate → `resume`) was needed to get a fully clean, correctly-encoded topic.
4. **[`debezium-default-key-breaks-partition-affinity.md`](../error-handling/debezium-default-key-breaks-partition-affinity.md)** — Debezium's default Kafka record key is the table's primary key (`event_id`), completely unrelated to ADR-0001's "partition by `apartment_id`" ordering requirement — 10 of 12 apartments were scattered across all 6 partitions. Fixed with `message.key.columns`, which only changes the key/partition assignment, leaving the payload's `event_id` untouched for Flink's upsert semantics.

**Environment note for reproducing this locally:** the custom converter JAR is *not* committed (see `.gitignore`) — rebuild it with:
```bash
docker run --rm -v "$(pwd)/infra/debezium/custom-converters:/app" -w /app \
  maven:3.9-eclipse-temurin-17 mvn -q -DskipTests package
```
before bringing up `docker-compose.yml`'s `kafka-connect` service, which volume-mounts the built JAR into the connector's plugin directory.

---

## Phase 3 — Market ingestion

**Status:** Implemented — AC-01 through AC-06 verified live against a running LocalStack stack on 2026-07-25, both via a direct `uv run` invocation and the real `services/market-ingestor/Dockerfile` built and run through `infra/docker-compose.yml`. **Spec:** [`specs/phases/03-market-ingestion/spec.md`](../specs/phases/03-market-ingestion/spec.md). **Diagram:** [`diagrams/phase-3-market-ingestion.md`](../diagrams/phase-3-market-ingestion.md). Not yet on its own branch — built directly on `main` this session (see "Where things stand," below).

**What was built:** `services/market-ingestor` — a synthetic market-price generator publishing `market_price.v1` snapshots to the `market-price-events` Kinesis stream (4 shards, provisioned in Phase 0's `infra/localstack/init-aws.sh`) on a fixed interval, plus the first real implementation in `libs/shared-schemas` (a hand-written `MarketPrice` Pydantic model, validated against the raw JSON Schema during this session, not just against itself).

**Key decisions, discovered and resolved before implementation (spec-driven, per this project's own practice):**

- **[ADR-0005](adr/ADR-0005-market-price-partition-key.md)** — ADR-0001 required partitioning both streams by `apartment_id`, but `market_price.v1` has no such field; it's scoped by market segment (city/neighborhood/property-profile), not an individual apartment. Resolved by partitioning `market-price-events` on a plain string built from the segment (`city|neighborhood|type|bedrooms`) — no application-level hash, Kinesis hashes it internally. A coarser key (city only) was considered and rejected: shard count is fixed at stream creation, so fewer distinct key values generally *worsens* concentration rather than improving it, while giving Flink no additional locality it actually needs.
- **Log-normal price sampling, not independently-randomized percentiles** (spec §5.2) — `avg_nightly_rate`/`p25`/`p50`/`p75` are derived from a synthetic per-listing price sample drawn from a log-normal distribution (median anchored to 2025–2026 market observations researched for Barcelona/Madrid/Valencia neighborhoods), not generated as four separate random numbers, which risked incoherent output like `p75 < p25` with nothing preventing it.
- **Bounded retry + log-and-drop for `put_records` partial failures** (spec §4) — framed explicitly in Kleppmann's delivery-semantics terms: retry is at-least-once delivery (resending identical bytes, never regenerating the event), the log-and-drop fallback after exhausting retries is at-most-once and only fires as a terminal case. True exactly-once delivery isn't achievable at this layer; what's actually relied on is at-least-once delivery plus idempotent consumption (§3: Phase 4 must compare `collected_at` across events for the same segment/date, mirroring ADR-0003's pattern for `payment_line` but for a different reason — `market_price`'s `event_id` is a pure replay-dedup key, not a stable row identity like `payment_line`'s).

**Verified live, not just unit-tested:** every generated event validated against the real `specs/events/market_price.v1.json` (not just the hand-written Pydantic model); the retry logic was exercised against fake clients simulating transient and persistent `put_records` failures; and — the most valuable confirmation — reading all 4 shards back after several ticks showed a `30/30/10/20` record distribution, the ADR-0005 hot-shard prediction actually observed, not just theorized.

**Genuinely open problem surfaced for Phase 4, not solved here:** nothing in the project maps an `apartment_id` to a market segment. `payment_lines.sql` (Phase 1) only has `apartment_id`/`apartment_reference` — no city, neighborhood, or property-profile columns. Phase 4 cannot join `payment-events.v1` against `market-price-events` without resolving this first — see spec §8 for candidate approaches, none chosen yet.

---

## Phase 4 — Flink processing

**Status:** Implemented and verified live end-to-end against a running cluster (Kafka, LocalStack DynamoDB/S3, RocksDB+S3 checkpointing). **Spec:** [`specs/phases/04-flink-processing/spec.md`](../specs/phases/04-flink-processing/spec.md). **Pre-spec:** [`docs/phase-4-streaming-design-decisions.md`](phase-4-streaming-design-decisions.md) (decisions A–H). **Diagram:** [`diagrams/phase-4-flink-processing.md`](../diagrams/phase-4-flink-processing.md). Merged to `main` via PR [#7](https://github.com/moradabaz/pms-price-engine/pull/7) (`48c1669`). See also the profitability floor reform below, merged via PR [#8](https://github.com/moradabaz/pms-price-engine/pull/8) (`1b95f28`).

**What was built:**
- [`apartment_market_segments.sql`](../specs/phases/01-mock-app-db/apartment_market_segments.sql) (Decision C.1) — the apartment→market-segment mapping Phase 3 left as an open problem. Seeded once from `mock-pm-app`'s own apartment pool, which now carries real `city`/`neighborhood`/`property_type`/`bedrooms` attributes (restricted to the 3 cities `market-ingestor` actually prices). Self-healing: `mock_pm_app.migrations` runs the same idempotent DDL at every startup, not just via `docker-entrypoint-initdb.d` — needed because Postgres only runs initdb scripts against an empty data directory, and this table didn't exist on volumes from earlier phases. Wired into `infra/debezium/postgres-connector.json`'s `table.include.list` and a second `RegexRouter` transform, publishing to `apartment-market-segments.v1`.
- `services/market-ingestor` follow-up (D.1/D.2): random per-segment `target_date` replaced with a deterministic cyclic offset shared by every segment per tick (full 60-day calendar coverage, refreshed hourly in steady state); a seasonality multiplier (verano ×1.30, hombro ×1.05, invierno ×0.85) applied before log-normal sampling.
- `streaming/flink-jobs/` — the two-stage PyFlink job itself: apartment-keyed cost aggregation + broadcast segment/margin enrichment (Stage A, `stage_cost_enrichment.py`), re-keyed into a segment-keyed cost⋈market cross-join with fan-out, a dead-man's-switch freshness watchdog, and the pricing formula (Stage B, `stage_price_decision.py`). Single sink: DynamoDB (`dynamodb_sink.py`), idempotent by `decision_id`. Runs as a real Docker image (`pms-flink-pyflink:2.3.0`, built from `streaming/flink-jobs/Dockerfile`) inside `infra/docker-compose.yml`'s `flink-jobmanager`/`flink-taskmanager`.
- `services/kinesis-kafka-bridge` — a small new service, not anticipated in the original spec: republishes `market-price-events` (Kinesis) onto a new Kafka topic, `market-price-bridge.v1`, unmodified, so Stage B can read it via `KafkaSource`. See ADR-0008 below.
- `PaymentLine` and `PriceDecision` Pydantic models added to `libs/shared-schemas`, alongside the existing `MarketPrice` — validated against every `specs/contracts/` fixture before use.

**Key decisions:**
- **[ADR-0006](adr/ADR-0006-dynamodb-single-writer-iceberg-cdc.md)** — no dual-write from Flink to both DynamoDB and Iceberg (Kleppmann's dual-write problem). Flink writes only to DynamoDB; Iceberg is populated by a separate CDC consumer reading DynamoDB Streams, owned by Phase 5.
- **[ADR-0007](adr/ADR-0007-price-decision-cost-protected-rule.md)** — `price_decision.v1`'s `rule_applied` gains a third value, `cost_protected` (the cost floor pushed the price above the raw market average, not just above the discounted reference price) — the first change to a contract an earlier phase had already treated as closed.
- **[ADR-0008](adr/ADR-0008-kinesis-kafka-bridge.md)** — discovered only once real connector JARs were wired up: PyFlink 2.3's Kinesis connector is the legacy `FlinkKinesisConsumer`, which extends `RichParallelSourceFunction`, part of the `SourceFunction` API Flink 2.0 removed — confirmed by inspecting the actual jar's bytecode, not assumed, and no released connector targets the new Source API for Kinesis yet. Resolved with `kinesis-kafka-bridge` rather than downgrading the cluster to Flink 1.20 or blocking on an unreleased connector.
- **`available_days`**, required by `price_decision.v1` since it was first drafted, was never actually resolved by any phase — nothing in this project tracks apartment availability/blocking. The Fase 4 spec (§14) makes the simplification explicit (full billing-period length) instead of leaving it an implicit assumption.

**Non-obvious findings — read before touching `streaming/flink-jobs/`, its Dockerfile, or `infra/docker-compose.yml`'s Flink services again:**
- [`flink-operational-checklist.md`](../error-handling/flink-operational-checklist.md) — 10 PyFlink gotchas, including a dead-man's-switch timer bug caught only by testing (full-precision `datetime` deadlines never matching Flink's millisecond-resolution timers) and the confirmed absence of a native Python `Sink` API (the DynamoDB writer is a `MapFunction` terminated by a Java `DiscardingSink`, moved to `...sink.legacy.DiscardingSink` in Flink 2.3 — also confirmed by inspecting the jar).
- [`flink-2x-removes-legacy-sourcefunction-breaking-flinkkinesisconsumer.md`](../error-handling/flink-2x-removes-legacy-sourcefunction-breaking-flinkkinesisconsumer.md) — the root-cause trace behind ADR-0008.
- [`pyflink-docker-image-build-gotchas.md`](../error-handling/pyflink-docker-image-build-gotchas.md) — 5 build issues (non-root user, `uv`'s managed Python install directory being unreadable at runtime, missing `build-essential`), each confirmed live, not guessed.
- [`full-pipeline-live-deployment-bugs.md`](../error-handling/full-pipeline-live-deployment-bugs.md) — 5 bugs only visible once the whole stack ran together: a fresh consumer group's `NoOffsetForPartitionException`, the `DiscardingSink` package move, a DynamoDB `Item` double-wrap, TaskManager missing AWS credentials the JobManager had, and a disappearing S3 bucket.
- [`stage-b-cost-side-fanout-can-emit-for-already-past-nights.md`](../error-handling/stage-b-cost-side-fanout-can-emit-for-already-past-nights.md) — found in a post-deployment code review: Stage B's cost-side fan-out doesn't sweep expired nights the way its market-side does, so it can (rarely, and not yet observed) emit a `price_decision` for a `target_date` already in the past. Not fixed — doing so changes the emission count `AC-03` pins, so it's a decision for the next spec revision, not a silent behavior change.

**Verified live, not just unit-tested:**
- **AC-08 (checkpointed restart):** killed the TaskManager mid-run via `docker kill`; Flink's own checkpoint API confirmed `"latest restored"` pointed at a real S3 checkpoint; `BCN-001`'s `cost_lines_count` grew across the failure (not reset, not duplicated).
- **AC-09 (freshness watchdog):** temporarily lowered `FRESHNESS_THRESHOLD_MS` to 30s, wired the previously-unconsumed `DATA_STALE_TAG` side output to a permanent `.print()` sink (a real gap — it had no consumer at all before this), and observed `data_stale` firing correctly for both "apartment" and "night" keys across all 4 parallel subtasks. Reverted the threshold to 48h afterward; `git diff` confirmed a clean revert.
- The full pipeline end-to-end: Stage A → Stage B → DynamoDB sink, writing 190+ real `price_decision` items from live Kafka/bridge data.
- **AC-11 (`apartment_market_segments` CDC):** inserted a new row directly via `psql`, confirmed the topic's latest offset advanced (10→11), and read the new message back — every field matched the inserted row exactly, including the schema's `target_margin`/`competitiveness_discount` defaults. Test row deleted afterward.
- **AC-03/AC-04 (fan-out shape):** the MiniCluster smoke test spec §13 committed to (and this diary previously flagged as missing) now exists — `tests/test_minicluster_smoke.py` runs Stage B's real `connect()`/`keyBy` wiring, asserting the exact M×N cost×market cross product. Sanity-checked by deliberately breaking the `keyBy` and confirming it fails (0 decisions instead of 6) before restoring it.
- 31 unit/component/MiniCluster tests (`uv run --package flink-jobs pytest streaming/flink-jobs/tests`), including a symmetry test and a MiniCluster smoke test added during a post-deployment senior review, plus a regression test for a clock-skew edge case (`data_age_seconds` clamped to 0, since a negative value would fail `MarketInputs`' `ge=0` validation and crash the task).

**Resolved:** the live `rule_applied` mix skewing entirely toward `market_competitive` was root-caused — Kafka's own topic retention had already expired `payment-events.v1`'s older segments before this consumer group's first read, making Stage A's cost aggregation partial (not wrong) for apartments whose invoice history predated the retention window, understating cost for every apartment. Written up in [`error-handling/kafka-retention-already-expired-old-payment-lines-history.md`](../error-handling/kafka-retention-already-expired-old-payment-lines-history.md).

### Follow-up: profitability floor reform (ADR-0009)

**Status:** Implemented, unit/contract-tested, and live-verified. Merged to `main` via PR [#8](https://github.com/moradabaz/pms-price-engine/pull/8) (`1b95f28`).

Stakeholders supplied a profitability model meant to sit as a financial guardrail under the market-driven price: the market engine proposes a price, a profitability floor sets the minimum acceptable one, and the system publishes `max(market, floor)`. Comparing it against the pricing formula shipped in the original Phase 4 surfaced real gaps, all closed per **[ADR-0009](adr/ADR-0009-profitability-floor-reform.md)**:

- **The floor formula was multiplying (`cost × (1 + margin)`) instead of dividing (`cost / (1 - margin)`)** — the exact "common error" the stakeholders' own reference material calls out; multiplying understates the real margin on the final price.
- **Commissions (`Cp`) didn't exist anywhere** — added per apartment (`commission_pct`, default `0.15`), in the floor's denominator alongside margin, since it scales with the final price.
- **Cost was already categorized (`payment_line.v1`'s `cost_type`: fixed/variable/one_time) but blended into one `daily_cost_eur`** — now split, with one-time costs **averaged** across the period rather than summed (each line is already the cost of one turnover; summing a month's worth would overstate it).
- **A single floor became two**, selected by days-to-arrival: structural (full or reduced margin, for bookings far out) vs. contribution (imminent dates, margin excluded, fixed cost excluded since it's sunk regardless of occupancy).

**Explicitly deferred, not silently dropped:** real per-stay-length pricing and per-channel pricing, both of which would change `price_decision.v1`'s primary key and multiply Stage B's fan-out volume — tracked in [`docs/post-poc-roadmap.md`](post-poc-roadmap.md) for whenever the PoC is ready to take that on.

**Verified live, not just unit-tested (2026-08-03):**
- Confirmed the `commission_pct` column actually lands on an *existing* `apartment_market_segments` table via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` (both the spec's SQL and `mock_pm_app.migrations`'s embedded copy), and that a real row update produces a CDC message carrying the new field.
- **Found and fixed a real bug during this verification:** the job crash-looped with `KeyError: 'commission_pct'` — `apartment_market_segments`' Kafka source replays from the earliest offset, and CDC messages predating the column's addition don't carry it at all. Fixed with a fallback to the same default as the DB column; regression test added (`tests/test_job.py`).
- Redeployed the fixed job against the live cluster and confirmed a real `price_decision` (apartment `BCN-006`, night `2026-09-12`) matches the ADR-0009 formula exactly by hand: `Cf=15.7, Cv=22.1, Cr=129.27, Cp=0.15, M=0.05` → `minimum_price_eur=208.84`, `rule_applied=cost_protected`, `effective_margin=0.25`.
- The `structural_reduced_margin`/`contribution` tiers weren't hit live in this session (`market-ingestor`'s 60-day cyclic coverage hadn't looped back to near-term dates yet) — covered exhaustively instead by `tests/test_pricing.py::test_floor_type_boundaries` and per-tier hand-verified cases.
- **Known gap at merge time, since fixed:** the bugfix commit above was pushed to the PR branch after the PR's merge button had already been used, so `main` briefly carried the crash-looping bug post-merge. Recovered via a small follow-up branch (`fix-commission-pct-cdc-backcompat`) cherry-picking the missed commit — see "Where things stand" below for its merge status.

---

## Phase 5 — Persistence (Iceberg + dbt)

**Status:** Done. PR1 merged ([#11](https://github.com/moradabaz/pms-price-engine/pull/11)); PR2 (dbt) built, unit-tested, live-verified end-to-end (2026-08-05), and merged ([#12](https://github.com/moradabaz/pms-price-engine/pull/12)). **Spec:** [`specs/phases/05-persistence/spec.md`](../specs/phases/05-persistence/spec.md). **Pre-spec:** [`docs/phase-5-persistence-design-decisions.md`](phase-5-persistence-design-decisions.md) (decisions A–K, includes live verification of DynamoDB Streams on LocalStack, 2026-08-03: single static shard, stream ARN doesn't survive a container restart — see the pre-spec's §F for detail, candidate for its own `error-handling/` write-up once actually implemented and hit again). Consumes Phase 4's `price_decision.v1` output into S3 + Iceberg (LocalStack locally) via a DynamoDB Streams CDC consumer, not direct consumption of a Flink-side event stream ([ADR-0006](adr/ADR-0006-dynamodb-single-writer-iceberg-cdc.md), confirmed 2026-07-29: Flink writes only to DynamoDB, never a second sink). dbt (`dbt-duckdb`) models on top of the raw Iceberg table (2 dimensions, 3 facts — price evolution, margin alerts per README).

**Confirmed 2026-08-04, before implementation:**
- **Implementation split into two PRs**, mirroring the spec's own "two independently-failing halves" design (§3): PR1 = `services/lakehouse-consumer` + `lakehouse-maintenance` + the missing LocalStack provisioning (`stream_checkpoints` table, `pms-lakehouse` bucket, `pms_lakehouse` Glue database — none of which exist yet in `infra/localstack/init-aws.sh`). PR2 = `transform/` (dbt project). The consumer is the real blocker — dbt without real Iceberg data would only ever test fixtures, not the actual system.
- **dbt run interval set to 15 minutes**, not the pre-spec's proposed 5 minutes — the dashboard's current-price view reads DynamoDB directly (hot path), so marts (history/margin-alerts only) don't need minute-level freshness; 15 min cuts Glue/S3 API call volume ~3x, relevant under Phase 7's real AWS cost guardrails. Tick loop must be "run, then sleep the remainder," not a fixed sleep, to avoid overlapping a slow run.
- **No dedicated ADR for Phase 5's catalog/partition/maintenance choices** — same criterion Phase 4 used: ADRs are for decisions that break a contract or cross phase boundaries (dual-write, `rule_applied`), not internal-to-the-phase infra choices already captured in the pre-spec doc.
- **IAM permissions section added to spec §10.1** — not enforced by LocalStack Community today, but written now (least-privilege table per service/resource) so Phase 7's real AWS promotion has something to implement against instead of improvising credentials at deploy time.

**PR1 built (2026-08-04): `services/lakehouse-consumer`, `services/lakehouse-maintenance`, LocalStack provisioning.**

- **What was built:** `lakehouse-consumer` (dynamic stream ARN resolution, parent-before-child shard ordering, per-shard checkpointing in a new `stream_checkpoints` DynamoDB table, merge-by-`decision_id` into an Iceberg table via PyIceberg) and `lakehouse-maintenance` (scheduled compaction, run-then-sleep-remainder tick loop). `infra/localstack/init-aws.sh` now also provisions `stream_checkpoints` and the `pms-lakehouse` S3 bucket (idempotently — see below); `infra/docker-compose.yml`'s `localstack` service enables `dynamodbstreams` (previously only `kinesis,s3,dynamodb` — Streams was silently unavailable) and gained both new service blocks plus a `lakehouse_catalog` volume.
- **Real dependency conflict found and resolved, not assumed:** `pyiceberg>=0.9` requires `pyarrow>=17`, which conflicts with `flink-jobs`' `apache-beam` pin (`pyarrow<17`) under this workspace's single lockfile (Phase 0 decision). Pinned `pyiceberg[glue,pyarrow,sql-sqlite]>=0.8.0,<0.9.0` instead — resolves cleanly, confirmed `flink-jobs`' own 39 tests still pass unaffected after the change.
- **Finding: the pinned `pyiceberg==0.8.1` has no `rewrite_data_files()`/`expire_snapshots()` at all** — confirmed by grepping the installed package's source directly, not assumed from the pre-spec's "0.7+ ships these" claim, which was wrong for the version this workspace can actually run. Compaction is implemented instead as a manual full-table rewrite (`table.scan().to_arrow()` + `table.overwrite(...)`, same net effect for AC-06) using APIs that do exist in 0.8.1, **live-verified**: 3 small data files → 1 file, same 3 rows, same content. `expire_snapshots` deferred as a documented known limitation (spec §13). Write-up: [`error-handling/pyiceberg-native-compaction-unavailable-under-shared-lockfile-pyarrow-pin.md`](../error-handling/pyiceberg-native-compaction-unavailable-under-shared-lockfile-pyarrow-pin.md).
- **Bigger finding, live-verified: AWS Glue Data Catalog is Ultimate-tier only in LocalStack — not available in Community at all**, not just Glue Jobs as the pre-spec's Decision C assumed (`GetDatabase`/`CreateDatabase` both 501 "pro feature", confirmed against LocalStack's own coverage docs). Switched both services' Iceberg catalog from `GlueCatalog` to PyIceberg's `SqlCatalog` (SQLite on the new `lakehouse_catalog` volume) for local/PoC use — storage (S3) unaffected, `build_catalog()` documents Phase 7's swap back to `GlueCatalog` against real AWS. Write-up: [`error-handling/localstack-glue-is-ultimate-tier-only-not-community.md`](../error-handling/localstack-glue-is-ultimate-tier-only-not-community.md).
- **`init-aws.sh` was not idempotent** — `PERSISTENCE=1` plus `set -e` meant a restart against an existing volume aborted on the very first `already exists` error, silently skipping every resource added after it (including all of Phase 5's new ones). Fixed with an existence-check-then-create pattern for every resource; verified live by restarting twice in a row. Write-up: [`error-handling/localstack-init-script-not-idempotent-against-persisted-state.md`](../error-handling/localstack-init-script-not-idempotent-against-persisted-state.md).
- **DynamoDB Streams orphaned after a LocalStack restart, and the pre-spec's own documented fix (disable/re-enable the stream) didn't fully work this time** — `describe_table` kept reporting a `LatestStreamArn` that `list-streams`/`describe-stream` didn't recognize, exactly `resolve_stream_arn`'s guard catching a real problem, not a false alarm. Resolved by deleting and recreating the table outright. Write-up: [`error-handling/dynamodb-streams-orphaned-after-localstack-restart-needs-full-table-recreation.md`](../error-handling/dynamodb-streams-orphaned-after-localstack-restart-needs-full-table-recreation.md).
- **Live-verified end-to-end (2026-08-04), not just unit-tested:** real `put-item` on `price_decision` → real DynamoDB Streams record → real consumer → real Iceberg table on real (LocalStack) S3, every nested field intact (**AC-01**). A same-`decision_id` replay with a different price updated the existing row instead of duplicating it (**AC-02**). `resolve_stream_arn` correctly rejected the orphaned post-restart ARN rather than silently reading nothing (**AC-03**'s failure-mode side proven live; the pre-spec's own recovery procedure needed escalating to a full table recreation, see above). `docker kill` on the consumer mid-stream, a third record written while it was down, then a restart: resumed from the DynamoDB-backed checkpoint and ended with exactly the 3 expected rows, no duplicates, no loss (**AC-04**). Manually triggered `compact_once` reduced 3 data files to 1 with the same 3 rows/content (**AC-06**). `ruff`, `mypy --strict`, and 20 unit/component tests also pass (AC-02/AC-05 covered there too, against a local SqlCatalog).
- **PR1 merged as [#11](https://github.com/moradabaz/pms-price-engine/pull/11)** — CI initially failed on `ruff format --check .` (whole-repo, not just the diff): two of the three flagged files were this PR's own (`checkpoint.py`, a code block in `spec.md`), the third (`streaming/flink-jobs/tests/test_job.py`) was already unformatted on `main` before this PR and fixed in its own separate, clearly-labeled commit rather than folded into Phase 5's history.
- **Refactor pass (2026-08-04), requested explicitly (clean code / DRY / modularization):** extracted `libs/lakehouse-shared` (`build_catalog()`, `IcebergCatalogSettings` — eliminates the identical catalog-wiring code both services previously duplicated) and split `lakehouse-consumer`'s `iceberg_writer.py` (previously mixing schema definition, NewImage→row transformation, and table lifecycle/merge) into `schema.py`/`transform.py`/`iceberg_writer.py`, each with one responsibility. Needed a `py.typed` marker in the new shared package — without it, mypy treats cross-package inheritance (`ConsumerSettings(IcebergCatalogSettings)`) as subclassing `Any` and refuses it. 22 tests passing after the refactor (was 20 — +2 for `lakehouse-shared` itself, previously untested since its logic lived duplicated, untested, inside each service). Re-verified live against the real running containers after rebuilding — a 4th decision (`BCN-004`) landed correctly through the refactored code path.

**PR2 built (2026-08-05): `transform/` (dbt project) + `services/dbt-runner`.**

- **What was built:** a `dbt-duckdb` project reading `price_decision_raw` directly via `iceberg_scan()` (no Glue, see below) — `stg_price_decision` (staging) → `int_latest_decision_per_night` (intermediate) → `dim_apartment`/`dim_date`/`fct_price_decision`/`fct_daily_price`/`fct_margin_alert` (marts, Kimball model per spec §8). `services/dbt-runner` is the tick container (`dbt seed`+`run`+`test` every 15 min, run-then-sleep-remainder loop). `specs/contracts/test_seasonality_contract.py` cross-checks `transform/seeds/seasonality.csv` against the live `market_ingestor.seasonality` module (AC-08), loaded by file path rather than package import so `specs`' own dependencies stay minimal.
- **Real dependency conflict found and resolved, not assumed:** no `dbt-core` version is compatible with both this workspace's `mypy>=2.1.0` pin and `flink-jobs`' `apache-beam` pin (`pathspec`/`protobuf` collide either way) under the single shared lockfile. Resolved by treating `dbt` as an external CLI: `dbt-core`/`dbt-duckdb` are installed into a separate venv baked into `dbt-runner`'s Docker image, invoked via `subprocess.run(["dbt", ...])`, never a declared dependency of `dbt-runner`'s own `pyproject.toml`. Write-up: [`error-handling/dbt-core-incompatible-with-shared-lockfile-mypy-and-flink-pins.md`](../error-handling/dbt-core-incompatible-with-shared-lockfile-mypy-and-flink-pins.md).
- **Verified live before building further, not assumed:** whether `dbt-duckdb` can actually read a table cataloged by PyIceberg's `SqlCatalog` (not Glue/REST) at all. Confirmed via a raw DuckDB spike against the real running LocalStack stack — `iceberg_scan()` needs `unsafe_enable_version_guessing` since `SqlCatalog` writes no version-hint file DuckDB can find on its own (only Hadoop-catalog convention does that). Also confirmed `dbt-duckdb`'s `meta.external_location` mechanism passes a raw `iceberg_scan('s3://...')` call through unquoted, by reading its adapter source directly rather than assuming.
- **Real spec gap found: `dim_apartment` can't have `property_type`/`bedrooms`** — `price_decision.v1`'s `market_area` is only `"City/Neighborhood"` (confirmed by reading `stage_price_decision.py`), never `property_type`/`bedrooms` (those live only in `apartment_market_segments`, Postgres, not a dbt source per spec §4). Pre-spec Decision K assumed data that was never actually forwarded that far. Confirmed with the user: `dim_apartment` ships with only `city`/`neighborhood` for now, documented as an explicit known limitation (spec §13), not silently dropped.
- **Real bug caught by live verification, not by tests:** `stg_price_decision.sql` initially referenced `market_area` as a top-level column; the real Iceberg schema nests it inside `market_inputs`. `dbt run` failed immediately against real data (`Binder Error: Referenced column "market_area" not found`) — fixed before merge. Also found the PR1 verification's own test data used an unrealistic `market_area` format (`"barcelona|eixample|apartment|2"`, pipe-delimited) that never matches what Flink actually produces (`"City/Neighborhood"`, slash-delimited) — inserted a fresh, realistic test row (`MAD-001`, `"Madrid/Salamanca"`, `rule_applied=cost_protected`) to properly exercise `dim_apartment`'s parsing and `fct_margin_alert`'s filter.
- **Live-verified end-to-end (2026-08-05):** `dbt seed`+`run`+`test` all succeed against the real running stack — 7 models (2 dims, 3 facts, staging, intermediate), 1 seed, 17 data tests, all passing (**AC-07**). `seasonality.csv` cross-check passes for all 12 months (**AC-08**). Inserting a `cost_protected` decision produced exactly 1 row in `fct_margin_alert`, correctly excluding the other 4 (non-`cost_protected`) decisions (**AC-09**). `dbt source freshness` runs and correctly reports "fresh" against real `ingested_at` timestamps (**AC-10**'s mechanism confirmed; the actual warn/error-threshold trip wasn't observed — would need the consumer stopped for 20+ real minutes, not exercised this session, noted in spec §13).
- **PR2 merged as [#12](https://github.com/moradabaz/pms-price-engine/pull/12).**
- **Not yet done:** the actual stale-vs-fresh transition for AC-10 (mechanism confirmed live, threshold trip not exercised — spec §13, not correctness-critical).

---

## Phase 6 — Dashboard

**Status:** Built, unit-tested, and live-verified end-to-end (2026-08-06) — not yet merged, on branch `phase-6-dashboard`. **Spec:** [`specs/phases/06-dashboard/spec.md`](../specs/phases/06-dashboard/spec.md). **Pre-spec:** [`docs/phase-6-dashboard-design-decisions.md`](phase-6-dashboard-design-decisions.md) (decisions A–H, 5-advisor panel on A/B/C). Streamlit (`dashboard/`, previously just a Phase 0 skeleton) reading `price_decision` from DynamoDB directly (hot path, `Query` only) and `dim_apartment`/`fct_daily_price`/`fct_margin_alert`/`fct_price_decision` from the dbt-materialized DuckDB file (cold path).

**What was built:** `dashboard/src/dashboard/hot_path.py` (one `Query` per known apartment, never `Scan`), `marts.py` (short-lived read-only DuckDB connections with retry-on-lock-conflict), `app.py` (3 Streamlit tabs: current price, price evolution, margin alerts), `Dockerfile`, and a new `dashboard` service in `infra/docker-compose.yml` mounting the same `dbt_warehouse` volume `dbt-runner` writes to, read-only. Added `ingested_at`/`dynamodb_event_name` to `fct_price_decision.sql` (Phase 5's model) so the dashboard's freshness indicator (AC-06) never has to touch the Iceberg source itself.

**Key finding, resolved by a cross-process test before assuming either way (pre-spec Decision C):** a `read_only=True` DuckDB connection from one process does raise `duckdb.OperationalError` while a second, genuinely separate OS process holds a plain writable connection to the same file open — confirmed via `multiprocessing` in `dashboard/tests/test_marts.py`, not via a same-process simulation (which produces a different, less representative `ConnectionException`). Retry-with-backoff was implemented regardless, then confirmed live: 8 concurrent reads against the real `dashboard` container during a real `dbt run --full-refresh` in `dbt-runner` all succeeded without error — `dbt-duckdb`'s commits are fast enough (~0.25s per run here) that the real collision window rarely gets hit, but the retry stays as cheap insurance. Write-up: [`error-handling/duckdb-concurrent-read-blocks-on-writer-lock.md`](../error-handling/duckdb-concurrent-read-blocks-on-writer-lock.md).

**Unrelated infra found broken on stack startup, not caused by this phase's own code:** bringing the full stack up after weeks stopped, `lakehouse-consumer`/`lakehouse-maintenance` (Phase 5) crash-looped — a stale PyIceberg catalog volume, then (once that was fixed) the exact orphaned-DynamoDB-stream symptom Phase 5 had already documented. This time the documented single-table delete/recreate fix didn't resolve it either (it kept reproducing the same broken state) — had to escalate to resetting LocalStack's whole persisted-data volume. Write-up: [`error-handling/dynamodb-streams-delete-recreate-cycle-doesnt-fix-orphaned-stream.md`](../error-handling/dynamodb-streams-delete-recreate-cycle-doesnt-fix-orphaned-stream.md).

**Verified live end-to-end (2026-08-06), not just unit-tested:** seeded 9 real `price_decision` items (3 apartments × 3 nights, one `cost_protected`) directly into DynamoDB — same style of direct verification Phase 5 used for its own AC-01. `lakehouse-consumer` merged all 9 into `price_decision_raw` (real log: `shard_batch_merged, count=9`). A manual `dbt seed`+`run`+`test` (17/17 tests) rebuilt the marts correctly — `fct_margin_alert` contained exactly the 1 `cost_protected` row (AC-04/AC-09's invariant, confirmed again at this layer). Ran the dashboard's own `hot_path`/`marts` functions inside the real `dashboard` container against the real running DynamoDB/DuckDB: current price per apartment matched the most recent decision for each (AC-01), price evolution matched `fct_daily_price` row-for-row (AC-03), freshness returned a real `ingested_at` timestamp (AC-06). AC-05 covered above. AC-02 (`Query`, never `Scan`) enforced by a component test asserting on the real call shape, not re-verified live (nothing to observe live that the unit test doesn't already cover deterministically).

**Not yet done:** the `fct_price_decision` drill-down view (stretch goal, spec §7) — not built this session. PR not yet opened.

---

## Phase 7 — Demo & docs

**Status:** Not started. Final ADRs, architecture diagrams, lessons-learned writeup, and — per the README's cost guardrails — a real AWS demo deployment via Terraform that must be torn down (`terraform destroy`) immediately after, with AWS Budget alerts set at $5/$10 *before* touching any real AWS service.

---

## Where things stand right now

As of 2026-08-06: Phases 1–5 are merged to `main`. Phase 6 (dashboard) is built, unit-tested, and live-verified — on branch `phase-6-dashboard`, not yet opened as a PR.

1. **Immediate next step: open and merge Phase 6's PR.** `dashboard/` (hot path + cold path Streamlit app) — see the Phase 6 section above for what was built and verified. The `fct_price_decision` drill-down view (spec §7, stretch goal) is the one item explicitly left for a follow-up, not a blocker.
2. **Read `error-handling/` before touching `infra/localstack/init-aws.sh`, any `lakehouse-*` service, `transform/`/`dbt-runner`, or `dashboard/` again** — Phase 5 surfaced 6 real findings there, and Phase 6 added 2 more (a DynamoDB-stream orphan whose documented single-table fix didn't work a second time, needing a full LocalStack volume reset; and DuckDB's real cross-process read/write lock behavior) — re-discovering any of them would be pure wasted time. Phase 2/4's earlier error-handling docs (connector config, Flink operational checklist, Kinesis/Flink-2.x, Docker build gotchas) are still just as relevant to `streaming/flink-jobs/`.
3. **Local environment reminders:** Docker Desktop must be running; `uv sync` for Python deps; `psql`/`kcat`/AWS CLI for manual verification per the README; the Maven build step (Phase 2) is required once per environment to reproduce the custom date converter. Pure-logic test suites (`flink-jobs`, `lakehouse-consumer`, `lakehouse-maintenance`, `lakehouse-shared`, `dbt-runner`, `dashboard`, `specs/contracts`) need no Docker/cluster at all. If LocalStack's Kinesis mock ever OOM-crash-loops on startup, its persisted snapshot (`/var/lib/localstack/state/kinesis` inside the `localstack_data` volume) has likely grown too large for the mock's default heap — clearing it (synthetic PoC data, safe to lose) resolves it. If the stack has sat stopped for a while, `lakehouse-consumer`/`lakehouse-maintenance` are the most likely to need a LocalStack volume reset on the next `up` — see the two Phase 5/6 DynamoDB-stream/catalog write-ups before assuming a single-resource fix will hold.
4. **No automated integration tests exist for the CDC pipeline or for market ingestion** (Phase 2/3, deliberate scope decisions). Phase 4, 5, and 6 are exceptions — pure business logic has full unit-test coverage and acceptance criteria have all been checked live by hand each time. If a future phase's timeline allows it, automating a subset of these (topic/stream message counts, schema validation) would catch a regression faster than the manual process used so far.
5. **Next phase, not started:** Phase 7 (demo & docs) — final ADRs, architecture diagrams, lessons-learned writeup, and a real AWS demo deployment via Terraform (torn down immediately after). Stay-length-aware and per-channel pricing remain deferred post-PoC (`docs/post-poc-roadmap.md`).
